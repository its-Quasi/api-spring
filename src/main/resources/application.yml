spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/jobapp
    username: test
    password: test
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
